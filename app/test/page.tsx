// app/test/page.tsx など（.tsx）
import PhotoIcon from "@/components/history-card/PhotoIcon";
import yaml from "yaml";
import Card from '@/components/history-card/Card';

export default function TestPage() {
  return (
    <>
      テスト用のページです。
      見つからないことを祈って。。。
    </>
  );
}
